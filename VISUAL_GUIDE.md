# 🎯 VEDANTU CHAT DASHBOARD - VISUAL GUIDE

## System Architecture

```
┌─────────────────────────────────────────────────────────┐
│                   Your Google Sheet                     │
│         (Public - 1-aRmuDOSu38Oid975ZiAvS3XER...)      │
└─────────────────────┬───────────────────────────────────┘
                      │
              ┌───────▼────────┐
              │  Flask Backend │
              │  (app.py)      │
              └───────┬────────┘
                      │
         ┌────────────┼────────────┐
         │            │            │
    ┌────▼──┐  ┌─────▼────┐  ┌───▼─────┐
    │ Method 1│ │ Method 2 │  │ N8N     │
    │ API v4  │ │ CSV      │  │ Webhook │
    │(Primary)│ │(Fallback)│  │         │
    └────────┘  └──────────┘  └─────────┘
         │            │            │
         └────────────┼────────────┘
                      │
         ┌────────────▼───────────┐
         │   Cache (5 minutes)    │
         └────────────┬───────────┘
                      │
         ┌────────────┴──────────────────┐
         │                               │
    ┌────▼─────────┐          ┌─────────▼──────┐
    │   Dashboard  │          │  Chat Interface│
    │  (9 columns) │          │  (Context View)│
    └──────────────┘          └────────────────┘
         │                               │
    ┌────▼────────────┐      ┌──────────▼─────────┐
    │ Agent Login     │      │ Show Previous Chats │
    │ View All Chats  │      │ Load Context        │
    │ Click Open Chat │      │ Live Chat Area      │
    └─────────────────┘      └─────────────────────┘
```

---

## Dashboard Flow

```
┌──────────────────────────────────────┐
│   1. AGENT ACCESSES DASHBOARD        │
│   URL: localhost:5000/dashboard      │
└───────────┬──────────────────────────┘
            │
            ▼
┌──────────────────────────────────────┐
│   2. LOGIN PAGE APPEARS              │
│   Credentials: agent / agent123      │
└───────────┬──────────────────────────┘
            │
            ▼
┌──────────────────────────────────────┐
│   3. FLASK FETCHES GOOGLE SHEET      │
│   ├─ Try API v4 (Primary)            │
│   └─ Or CSV export (Fallback)        │
└───────────┬──────────────────────────┘
            │
            ▼
┌──────────────────────────────────────┐
│   4. DATA CACHED & DISPLAYED         │
│   ├─ Sno                             │
│   ├─ Chat ID                         │
│   ├─ Name                            │
│   ├─ Class                           │
│   ├─ Target Exam                     │
│   ├─ School Board                    │
│   ├─ Type                            │
│   ├─ Timestamp                       │
│   └─ Action (Open Chat)              │
└───────────┬──────────────────────────┘
            │
            ▼
┌──────────────────────────────────────┐
│   5. AGENT CLICKS "OPEN CHAT"        │
└───────────┬──────────────────────────┘
            │
            ▼
┌──────────────────────────────────────┐
│   6. CHAT INTERFACE LOADS            │
│   ├─ Fetches previous context        │
│   ├─ Shows previous conversations    │
│   └─ Shows live chat area            │
└──────────────────────────────────────┘
```

---

## Database/Sheet Structure

```
Your Google Sheet Layout:

┌─────┬───────────┬──────────────┬───────┬──────────┬──────────┐
│ Sno │ Chat ID   │ Name         │ Class │ Exam     │ Board    │
├─────┼───────────┼──────────────┼───────┼──────────┼──────────┤
│ 1   │ CHAT_001  │ Arjun Singh  │ 10    │ JEE      │ CBSE     │
│ 2   │ CHAT_002  │ Priya Patel  │ 12    │ NEET     │ CBSE     │
│ 3   │ CHAT_001  │ Arjun Singh  │ 10    │ JEE      │ CBSE     │
└─────┴───────────┴──────────────┴───────┴──────────┴──────────┘

Additional columns (hidden in table, used for context):
[Type] [User Intent] [AI Response] [Medium] [Location] [Doubt] 
[Free] [Ranking] [Transferred] [Timestamp] [State]
```

---

## Chat Context Display

```
┌─ PREVIOUS CONVERSATION CONTEXT ─────────────────┐
│                                                 │
│ Conversation 1:                                 │
│ ├─ User Intent:                                 │
│ │  "How to solve quadratic equations?"          │
│ │                                               │
│ ├─ AI Response:                                 │
│ │  "Use the quadratic formula: x = -b±√(...)"   │
│ │                                               │
│ ├─ Doubt: Quadratic Equations                   │
│ └─ State: Active                                │
│                                                 │
│ Conversation 2:                                 │
│ ├─ User Intent:                                 │
│ │  "NCERT solutions available?"                 │
│ │                                               │
│ ├─ AI Response:                                 │
│ │  "Yes, FREE on Vedantu"                       │
│ │                                               │
│ ├─ Doubt: NCERT                                 │
│ └─ State: Resolved                              │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## API Endpoint Diagram

```
┌─────────────────────────────────────────────────┐
│              API ENDPOINTS                      │
├─────────────────────────────────────────────────┤
│                                                 │
│ GET /api/chat-sessions                          │
│ ├─ Input: None                                  │
│ └─ Output: All chats from Google Sheet          │
│                                                 │
│ GET /api/chat-context/{chatId}                  │
│ ├─ Input: Chat ID                              │
│ └─ Output: Previous conversations               │
│                                                 │
│ POST /api/refresh-sheets                        │
│ ├─ Input: None                                  │
│ └─ Output: Success message                      │
│                                                 │
│ POST /api/send-message                          │
│ ├─ Input: Chat ID, message, sender              │
│ └─ Output: Message confirmation                 │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## Data Fetch Flowchart

```
User requests /api/chat-sessions
        │
        ▼
    Is cache valid?
    ├─ YES → Return cached data ✅
    └─ NO  → Fetch new data
           │
           ▼
       Try Google Sheets API v4
       ├─ Success (200) → Parse & cache ✅
       └─ Error (403/other) → Try Method 2
                  │
                  ▼
              Try CSV Export
              ├─ Success (200) → Parse & cache ✅
              └─ Error → Return error message ❌
```

---

## Component Interaction

```
┌────────────────────────────────────────────────────┐
│         Frontend (Vue.js)                         │
│ ┌──────────────┐              ┌──────────────────┐│
│ │  Dashboard   │              │ Chat Interface   ││
│ │  Component   │◄────────────►│  Component       ││
│ └──────────────┘              └──────────────────┘│
└────────────────────────────────────────────────────┘
         │                              │
         ▼                              ▼
    API Calls (fetch)              API Calls (fetch)
         │                              │
         ▼                              ▼
┌────────────────────────────────────────────────────┐
│      Backend (Flask + Python)                     │
│ ┌────────────────────────────────────────────────┐│
│ │ Route Handlers                                 ││
│ │ ├─ /api/chat-sessions                         ││
│ │ ├─ /api/chat-context                          ││
│ │ ├─ /api/refresh-sheets                        ││
│ │ └─ /api/send-message                          ││
│ └────────────────────────────────────────────────┘│
│ ┌────────────────────────────────────────────────┐│
│ │ Google Sheets Integration                      ││
│ │ ├─ get_google_sheets_data() - API v4          ││
│ │ ├─ get_google_sheets_csv()  - CSV export      ││
│ │ └─ parse_sheets_to_chats()  - Data mapping    ││
│ └────────────────────────────────────────────────┘│
│ ┌────────────────────────────────────────────────┐│
│ │ Cache Layer (5 minutes)                         ││
│ └────────────────────────────────────────────────┘│
└────────────────────────────────────────────────────┘
         │
         ▼
    Google Sheets API / CSV Export
         │
         ▼
    Your Public Google Sheet
```

---

## Message Flow in Chat

```
Agent Types Message
    │
    ▼
Form Submission
    │
    ├─ Display locally ✅
    │
    ▼
Send to /api/send-message
    │
    ├─ Forward to N8N webhook
    │ │
    │ └─ N8N processes
    │   │
    │   └─ Sends response back
    │
    ▼
Response arrives at /api/webhook/response
    │
    ├─ Store in pending_responses
    │
    ▼
Frontend polls /api/poll-messages
    │
    ├─ Get new messages
    │
    ▼
Display in Chat Interface ✅
```

---

## Dashboard Table Columns Explained

```
┌─────────────────────────────────────────────────────┐
│ Dashboard Table Header                              │
├─────────────────────────────────────────────────────┤
│ Sno          │ Sequential number from sheet         │
│ Chat ID      │ Unique chat identifier (CHAT_001)    │
│ Name         │ Student/Customer name                │
│ Class        │ Class/Grade (10, 11, 12, etc)        │
│ Target Exam  │ JEE, NEET, CBSE, etc                │
│ School Board │ CBSE, ICSE, State Board, etc        │
│ Type         │ Parent / Student / Visitor           │
│ Timestamp    │ When conversation happened           │
│ Action       │ "Open Chat" button                   │
└─────────────────────────────────────────────────────┘
```

---

## Performance Timeline

```
User Action          │  Time  │  What Happens
─────────────────────┼────────┼──────────────────────
Dashboard Load       │  1-2s  │ Fetches Google Sheet
(first time)         │        │ Parses data
                     │        │ Renders table
                     │        │ Caches result
─────────────────────┼────────┼──────────────────────
Dashboard Load       │ 200ms  │ Returns cached data
(subsequent)         │        │
─────────────────────┼────────┼──────────────────────
Chat Open            │ 500ms  │ Fetches context
                     │        │ Renders history
─────────────────────┼────────┼──────────────────────
Message Send         │ 100ms  │ Shows locally
                     │        │ Sends to N8N
─────────────────────┼────────┼──────────────────────
Message Receive      │  2-3s  │ Poll discovers it
                     │        │ Displays in chat
─────────────────────┼────────┼──────────────────────
Cache Refresh        │ 1-2s   │ Fetches fresh data
                     │        │ Updates display
```

---

## Error Handling Flow

```
Error Detected
    │
    ├─ API returns 403
    │ └─ Try CSV export ✅
    │
    ├─ CSV export fails
    │ └─ Show "Refresh Sheets" button 📌
    │
    ├─ Network timeout
    │ └─ Use cached data (if available) 💾
    │
    ├─ Invalid Sheet ID
    │ └─ Show configuration error ⚠️
    │
    └─ Column format error
      └─ Log error, show all available data 📋
```

---

## Login & Session Flow

```
Visit Dashboard
    │
    ▼
Check Session
├─ Valid? → Show Dashboard ✅
└─ Invalid? → Redirect to login
           │
           ▼
       Show Login Form
           │
           ▼
       User Enters Credentials
           │
           ▼
       POST /api/auth/login
           │
           ├─ Credentials match? ✅ → Set session
           └─ Invalid? ❌ → Show error
                       │
                       ▼
                   Show Dashboard
```

---

## Caching Strategy

```
┌──────────────────────────────────────┐
│      Request for Data                │
└───────────┬──────────────────────────┘
            │
            ▼
┌──────────────────────────────────────┐
│ Is cache valid? (< 5 minutes)        │
├──────────────────────────────────────┤
│ YES ✅                    NO ❌       │
│  │                         │         │
│  └─► Return Cache       Fetch Fresh  │
│       (200ms)           Data          │
│       │                 (1-2s)        │
│       │                  │            │
│       └──────┬───────────┘            │
│              │                        │
│              ▼                        │
│        Store in Cache                │
│        (lru_cache 5min)              │
│              │                        │
│              ▼                        │
│        Return Data                    │
└──────────────────────────────────────┘
```

---

**Diagram Created**: December 20, 2025  
**Version**: 1.0 Visual Reference Guide
